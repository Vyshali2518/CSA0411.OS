#include <stdio.h>

int main() {
    int n, frames, i, j, k, flag, pageFaults = 0;
    printf("Enter number of pages: "); scanf("%d", &n);
    int pages[n];
    printf("Enter page reference sequence: ");
    for(i=0;i<n;i++) scanf("%d",&pages[i]);
    printf("Enter number of frames: "); scanf("%d",&frames);
    int f[frames];
    for(i=0;i<frames;i++) f[i]=-1;

    int index = 0;
    for(i=0;i<n;i++){
        flag=0;
        for(j=0;j<frames;j++) if(f[j]==pages[i]) flag=1;
        if(flag==0){
            f[index]=pages[i];
            index=(index+1)%frames;
            pageFaults++;
        }
        printf("Frames: ");
        for(j=0;j<frames;j++) printf("%d ", f[j]);
        printf("\n");
    }
    printf("Total page faults: %d\n", pageFaults);
    return 0;
}
